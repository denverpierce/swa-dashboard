{
  "name": "airline-checker",
  "$schema": "https://raw.githubusercontent.com/SchemaStore/schemastore/master/src/schemas/json/package.json",
  "version": "0.1.0",
  "description": "Monitorer and receive alerts for changes in an airlines fare prices",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p ./tsconfig.json",
    "watch": "tsc -w",
    "prestart": "tsc -p ./tsconfig.json",
    "check": "node ./bin/index.js"
  },
  "dependencies": {
    "playwright": "^1.1.1",
    "typescript": "^3.9.2"
  },
  "devDependencies": {
    "@types/node": "^13.9.1"
  }
}
